// FAT ARROW FUNCTIONS: son una nueva forma que tenemos nosotros para poder escribir nuestras funciones, las sintaxis que tienen las fat arrows functions la verdad es usualmente más corta a lo cual nosotros estamos normalmente acostumbrados a escribir nuestras funciones

// function suma(a, b) {
//   return a + b;
// }

// const resultado = suma(1, 5);

// console.log(resultado);

const suma = (a, b) => a + b;

const resultado = suma(1, 5);

console.log(resultado);
