/**
 * SET Y MAPP
 *  - Permiten tener de una forma más indexada un tipo de objeto
 *
 *  - los maps van mejor para borrar y para agregar
 *  - objetos es mejor para busquedas
 *  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map
 */

const a = Object.create(null);

console.log(a);
