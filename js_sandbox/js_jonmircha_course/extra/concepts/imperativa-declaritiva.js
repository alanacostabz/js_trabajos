/**
 *
 * PROGRAMACIÓN IMPERATIVA Y DECLARATIVA
 *
 * - IMPERATIVA: es cuando nosotros le indicamos a la maquina que es lo que tiene que hacer (le indicamos el control de flujo)
 * cuando estamos utilizando todas las instrucciones que provee el lenguaje de programación en nuestro caso JS (if, if-else, switch, for,
 * foreach, switch-case)
 *
 * - DECLARATIVA: define la logica pero no detalla el control de flujo, le vantaje de este paragima es que es mas facil de leer
 * codigo
 *
 *
 * NOTA: La declarativa e imperativa coexisten, imperativa puede existir por si sola, pero la declarativa necesita de la programación
 * imperativa para poder existir
 */
